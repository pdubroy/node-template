{
  "name": "PACKAGE_NAME",
  "version": "0.1.0",
  "author": {
    "name": "Patrick Dubroy",
    "email": "pdubroy@gmail.com",
    "url": "http://dubroy.com"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^5.9.1",
    "jshint": "^2.5.2",
    "tape": "^2.13.4",
    "watchify": "^1.0.1"
  },
  "scripts": {
    "init-repo": "mkdir bin dist lib third_party; git remote rm origin; npm init && git reset --soft ceac6c && git add . && git commit --amend -m 'Initial commit.'",
    "build": "browserify -s $(pwd) -o dist/$(pwd)-bundle.js index.js",
    "watch": "watchify -s $(pwd) -o dist/$(pwd)-bundle.js index.js",
    "test": "tape test/*.js",
    "prepublish": "npm run build && npm run test && npm run lint",
    "lint": "jshint *.js lib/*.js test/*.js"
  }
}
