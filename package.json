{
  "name": "PACKAGE_NAME",
  "version": "0.1.0",
  "author": {
    "name": "Patrick Dubroy",
    "email": "pdubroy@gmail.com",
    "url": "http://dubroy.com"
  },
  "license": "MIT",
  "main": "main.js",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^5.9.1",
    "jshint": "^2.5.2",
    "tape": "^2.13.4",
    "watchify": "^1.0.1"
  },
  "scripts": {
    "init-repo": "mkdir bin dist lib third_party; git remote rm origin; git reset --soft ceac6c && git commit --amend -m 'Create project.'",
    "build": "browserify -s $(pwd) -o dist/$(pwd)-bundle.js main.js",
    "watch": "watchify -s $(pwd) -o dist/$(pwd)-bundle.js main.js",
    "test": "tape test/*.js",
    "prepublish": "npm run build && npm run test && npm run lint",
    "lint": "jshint *.js lib/*.js test/*.js"
  }
}
